// client-side game logic

// implement client-side predictions
// while waiting for Meteor.method-calls
  // guess what should happen

  // if correct prediction
    // keep game state, keep going

  // if not
    // use new data coming from server


// maybe this is already implemented using minimongo-mongo-syncing

// then just use Meteor.call("methodName", [parameters]);
// let client predict results
